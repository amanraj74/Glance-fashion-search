"""Streamlit demo for Glance Fashion Search.

Install:
    pip install streamlit

Run:
    streamlit run app/streamlit_app.py

Requires the image index to be built first:
    python indexer/build_index.py

Optional: build the caption index for hybrid retrieval:
    python scripts/build_caption_index.py
"""
