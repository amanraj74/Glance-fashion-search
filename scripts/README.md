# Application scripts (CLI entry points)

Utilities that orchestrate `glance_search` for common workflows.

- `caption_corpus.py` — generate BLIP captions for all images
- `run_eval.py` — run the 5 rubric queries and save grids
